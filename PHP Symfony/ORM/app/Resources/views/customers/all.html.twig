<table border="1">
    <tr>
        <th>Name</th>
        <th>Birth date</th>
        <th>Is young driver</th>
    </tr>
    {% for customer in customers %}
        <tr>
            <td>{{ customer.name }}</td>
            <td>{{ customer.birthDay|date("m/d/Y") }}</td>
            <td>{% if customer.isYoungDriver == 0 %}
                    false
                {% else %}
                    true
                {% endif %}</td>
        </tr>
    {% endfor %}
</table>